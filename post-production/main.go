package main

import (
	"os"
	"fmt"
	"log"
)

func init() {
	// Check if the SD is there.
	// IF the sd card is there than; check if the files are there.
	// remove the .lrf files
	// upload the mp4 files with the srt files to s3
}

func main() {
	//
}
